
### This is an auto-generated test suite, it needs to be completed with valid data.
### These are not all tests you need, more of them should be created to evaluate the functional
### requirements of your project. These tests only test the CRUD endpoints of the entity.
### Silence is a DEAL research team project, more info about us in https://deal.us.es
@BASE = http://127.0.0.1:8080/api/v1

### Auxiliary query
### Positive test
### Test 00: Get all existing album
### This query is used in several of the below tests it should be executed first.
# @name album
GET {{BASE}}/album

### Login a(n) users and save the generated token 
### This token is used in several of the below tests it should be executed second.
# @name login
POST {{BASE}}/login 
Content-Type: application/json 

{ 
	"username": ###REPLACE###,
	"password": ###REPLACE###
}

###
@token = {{login.response.body.sessionToken}}


### TESTS BEGIN

### Test 01: Get one existing album by its id.
### positive test 
@albId = {{album.response.body.0.albumId}}
GET {{BASE}}/album/{{albId}} 
Content-Type: application/json 


### Test 02: Try get one existing album by its nonexistent id.
### negative test 
GET {{BASE}}/album/999999999
Content-Type: application/json 


### Test 03: Add a new album successfully
### Positive test 
### We assume that the token has been aquired by the login request.
# @name newalbum
POST {{BASE}}/album 
Content-Type: application/json 
Token: {{token}} 

{
	"nombre": ###REPLACE###,
	"url": ###REPLACE###,
	"fecha_publicacion": ###REPLACE###,
	"spotify_id": ###REPLACE###,
	"destacado": ###REPLACE###,
	"artistaId": ###REPLACE###
} 

### Check the created album

@newalbumid = {{newalbum.response.body.lastId}}
GET {{BASE}}/album/{{newalbumid}}
Content-Type: application/json 

### Test 04: Add a new album without a session token
### Negative test 
POST {{BASE}}/album 
Content-Type: application/json 

{ 
	"nombre": ###REPLACE###,
	"url": ###REPLACE###,
	"fecha_publicacion": ###REPLACE###,
	"spotify_id": ###REPLACE###,
	"destacado": ###REPLACE###,
	"artistaId": ###REPLACE###
}

### Test 05: Modify an existing album 
### Positive test 
@albId = {{album.response.body.0.albumId}} 
PUT {{BASE}}/album/{{albId}} 
Content-Type: application/json 
Token: {{token}} 

{ 
	"nombre": ###REPLACE###,
	"url": ###REPLACE###,
	"fecha_publicacion": ###REPLACE###,
	"spotify_id": ###REPLACE###,
	"destacado": ###REPLACE###,
	"artistaId": ###REPLACE###
}

### Check the modified album

GET {{BASE}}/album/{{newalbumid}}
Content-Type: application/json 

### Test 06: Try to modify an existing album without a session token
### Negative test
@albId = {{album.response.body.0.albumId}}
PUT {{BASE}}/album/{{albId}}
Content-Type: application/json

{
	"nombre": ###REPLACE###,
	"url": ###REPLACE###,
	"fecha_publicacion": ###REPLACE###,
	"spotify_id": ###REPLACE###,
	"destacado": ###REPLACE###,
	"artistaId": ###REPLACE###
}

### Test 07: Delete an existing album
### Positive test

### Create a new album, which we will delete
# @name createdalbToDelete
POST {{BASE}}/album
Content-Type: application/json
Token: {{token}}

{
	"nombre": ###REPLACE###,
	"url": ###REPLACE###,
	"fecha_publicacion": ###REPLACE###,
	"spotify_id": ###REPLACE###,
	"destacado": ###REPLACE###,
	"artistaId": ###REPLACE###
}

### Check the created album
@albdelId = {{createdalbToDelete.response.body.lastId}}
GET {{BASE}}/album/{{albdelId}}
Content-Type: application/json 

### Delete the album
DELETE {{BASE}}/album/{{albdelId}}
Token: {{token}}

### Check the deleted album
GET {{BASE}}/album/{{albdelId}}
Content-Type: application/json 

### Test 08: Try to delete a album without a session token
### Negative test
DELETE {{BASE}}/album/{{albId}}

